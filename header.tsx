import { h } from "preact";

export default function MyHeader() {
    return (
      <div class={tw`p-4 mx-auto max-w-screen-md`}>
       <H1>
        This is my header :) 
       </H1>
      </div>
    );
}

